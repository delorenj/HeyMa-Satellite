#!/usr/bin/env bash
#MISE description="Run focused voice gateway and satellite checks"
set -euo pipefail
tonny_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../.." && pwd)"
exec python3 "$tonny_root/deploy/tonny/manage.py" check
