#!/usr/bin/env bash
#MISE description="Verify local containers, browser audio, and source reload"
set -euo pipefail
tonny_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../../.." && pwd)"
exec python3 "$tonny_root/deploy/tonny/local.py" check "$@"
