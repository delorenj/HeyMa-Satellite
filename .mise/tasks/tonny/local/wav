#!/usr/bin/env bash
#MISE description="Send a WAV through the container satellite"
set -euo pipefail
tonny_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../../.." && pwd)"
exec python3 "$tonny_root/deploy/tonny/local.py" wav "$@"
