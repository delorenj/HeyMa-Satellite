#!/usr/bin/env bash
#MISE description="Stop and remove the local Tonny stack"
set -euo pipefail
tonny_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../../.." && pwd)"
exec python3 "$tonny_root/deploy/tonny/local.py" down "$@"
