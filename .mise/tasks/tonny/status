#!/usr/bin/env bash
#MISE description="Show live gateway health and Tonny satellite state"
set -euo pipefail
tonny_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../.." && pwd)"
exec python3 "$tonny_root/deploy/tonny/manage.py" status
