#!/usr/bin/env bash
#MISE description="Record a six-second turn on Tonny and play the agent reply"
set -euo pipefail
tonny_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../.." && pwd)"
exec python3 "$tonny_root/deploy/tonny/manage.py" talk
