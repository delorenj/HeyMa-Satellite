#!/usr/bin/env bash
#MISE description="Deploy committed Pipecat/Line gateway and Tonny audio satellite"
set -euo pipefail
tonny_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../.." && pwd)"
exec python3 "$tonny_root/deploy/tonny/manage.py" deploy
